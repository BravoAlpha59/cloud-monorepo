# macOS
.DS_Store

# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# uv / build artifacts — requirements.txt files are generated by
# `uv export --frozen --no-dev` during `make build`; never commit them.
requirements.txt

# SAM build artifacts
.aws-sam/

# Chat summaries exported from OneNote — local reference only,
# may contain account IDs or pre-finalized naming.
docs/chat-summaries/

# Editor
.vscode/
.idea/
